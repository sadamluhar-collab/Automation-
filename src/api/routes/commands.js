import {commandCenter} from '../../commands/command-center.js';import {auditCommand} from '../../commands/command-audit.js';export async function execute(req,res){try{const data=await commandCenter.execute(req.body.command,req.body.job_id);await auditCommand({user_id:req.user.id,job_id:req.body.job_id,command:req.body.command,new_state:data.status,request_id:req.requestId});res.json({success:true,data})}catch(e){res.status(e.status||400).json({success:false,error:{code:e.code||'COMMAND_FAILED',message:e.message,request_id:req.requestId}})}}
