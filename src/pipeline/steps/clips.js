export async function run(ctx){if(!ctx.providers?.video)throw new Error('Video provider not configured');const clips=ctx.input?.clips||[];const out=[];for(let i=0;i<clips.length;i++){if(ctx.checkpoint?.clip_index>=i)continue;out.push(await ctx.providers.video.generate({...clips[i],duration:5,idempotencyKey:`${ctx.job.id}:clip:${i}`}));if(ctx.checkpoint)ctx.checkpoint.clip_index=i}return out}
