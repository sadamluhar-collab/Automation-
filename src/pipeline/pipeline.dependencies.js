const deps={research:[], 'content-plan':['research'],script:['content-plan'],scenes:['script'],references:['scenes'],clips:['references'],audio:['script'],music:['audio'],assembly:['clips','audio','music'],thumbnail:['assembly'],qc:['assembly','thumbnail'],upload:['qc'],analytics:['upload']};export const dependencies=(step)=>deps[step]||[];export function downstream(step){const out=[];let changed=true;while(changed){changed=false;for(const [k,v] of Object.entries(deps))if((v.includes(step)||v.some(x=>out.includes(x)))&&!out.includes(k)){out.push(k);changed=true}}return out}}
