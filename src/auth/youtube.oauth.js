import {env} from '../config/env.js';const scope='https://www.googleapis.com/auth/youtube.upload https://www.googleapis.com/auth/youtube.readonly';export function authorizationUrl(state){const e=env();const q=new URLSearchParams({client_id:e.YOUTUBE_CLIENT_ID,redirect_uri:e.YOUTUBE_REDIRECT_URI,response_type:'code',scope,access_type:'offline',prompt:'consent',state});return `https://accounts.google.com/o/oauth2/v2/auth?${q}`}
export async function exchangeCode(code){const e=env();const r=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({code,client_id:e.YOUTUBE_CLIENT_ID,client_secret:e.YOUTUBE_CLIENT_SECRET,redirect_uri:e.YOUTUBE_REDIRECT_URI,grant_type:'authorization_code'})});if(!r.ok)throw new Error('YouTube OAuth exchange failed');return r.json()}
