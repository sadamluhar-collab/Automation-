import {env} from '../config/env.js';export async function broadcast(topic,event,payload){const e=env(),key=e.SUPABASE_SERVICE_ROLE_KEY;if(!key)throw new Error('Realtime service key missing');const r=await fetch(`${e.SUPABASE_URL}/realtime/v1/api/broadcast`,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({messages:[{topic,event,payload}]})});if(!r.ok)throw new Error(`Realtime broadcast ${r.status}`);return true}
