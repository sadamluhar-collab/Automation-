import {env} from '../config/env.js';export async function upload(bucket,path,bytes,contentType='application/octet-stream'){const e=env(),key=e.SUPABASE_SERVICE_ROLE_KEY;if(!key)throw new Error('Storage service key missing');const r=await fetch(`${e.SUPABASE_URL}/storage/v1/object/${bucket}/${path}`,{method:'POST',headers:{Authorization:`Bearer ${key}`,apikey:key,'Content-Type':contentType},body:bytes});if(!r.ok)throw new Error(`Storage upload ${r.status}`);return {bucket,path}}
