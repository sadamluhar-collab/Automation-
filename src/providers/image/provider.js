export function imageProvider({url,apiKey}){return {async generate(input){if(!url||!apiKey)throw Object.assign(new Error('Image provider credentials missing'),{code:'CONFIGURATION'});const r=await fetch(url,{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify(input)});if(!r.ok)throw new Error(`Image provider HTTP ${r.status}`);return r.json()}}}
