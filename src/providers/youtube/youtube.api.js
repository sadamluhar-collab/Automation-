export async function youtubeRequest(accessToken,path,init={}){const r=await fetch(`https://www.googleapis.com/youtube/v3/${path}`,{...init,headers:{Authorization:`Bearer ${accessToken}`,'Content-Type':'application/json',...(init.headers||{})}});const t=await r.text();let d;try{d=JSON.parse(t)}catch{d=t}if(!r.ok)throw Object.assign(new Error(`YouTube API ${r.status}`),{code:r.status===401?'AUTH':'YOUTUBE',details:d});return d}
