export function videoProvider({url,apiKey}){return {async generate(input){if(!url||!apiKey)throw Object.assign(new Error('Video provider credentials missing'),{code:'CONFIGURATION'});const r=await fetch(url,{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json','Idempotency-Key':input.idempotencyKey||''},body:JSON.stringify(input)});if(!r.ok)throw Object.assign(new Error(`Video provider HTTP ${r.status}`),{code:r.status===429?'RATE_LIMIT':'PROVIDER'});return r.json()}}}
