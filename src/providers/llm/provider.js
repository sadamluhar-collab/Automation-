export function llmProvider({url,apiKey,model}){return {async generate(input){if(!url||!apiKey)throw Object.assign(new Error('LLM provider credentials missing'),{code:'CONFIGURATION'});const r=await fetch(url,{method:'POST',headers:{Authorization:`Bearer ${apiKey}`,'Content-Type':'application/json'},body:JSON.stringify({model,input})});if(!r.ok)throw new Error(`LLM provider HTTP ${r.status}`);return r.json()}}}
