import {backoff} from '../utils/retry.js';import {updateJob} from './queue.service.js';export async function retryJob(job,err){const n=(job.retry_count||0)+1;if(n>(job.max_retries??3))return false;await updateJob(job.id,{status:'retrying',retry_count:n,next_attempt_at:new Date(Date.now()+backoff(n)*1000).toISOString(),error_code:err.code||'RETRYABLE',error_message:err.message});return true}
