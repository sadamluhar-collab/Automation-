export function classify(e){const m=String(e?.message||'').toLowerCase();if(e?.code==='AUTH'||m.includes('token'))return 'AUTH';if(m.includes('timeout')||m.includes('network'))return 'NETWORK';if(m.includes('supabase')||m.includes('database'))return 'DATABASE';if(m.includes('youtube'))return 'YOUTUBE';if(m.includes('provider'))return 'PROVIDER';if(m.includes('queue'))return 'QUEUE';return 'UNKNOWN'}
