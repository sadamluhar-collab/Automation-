# Providers
All external AI/media services are adapters behind capability-aware routing. Configure provider credentials server-side. A missing credential or provider failure is surfaced as a real configuration/provider fault; the system never fabricates successful output.
