# Testing
Unit tests run with Node's built-in test runner. Integration and E2E suites are environment-gated rather than fake. CI should provision a disposable Supabase project and provider credentials for full external tests.
