# Security
Use Supabase Auth, RLS, tenant checks, server-side command validation, request IDs, rate limits and audit records. Keep service keys and OAuth refresh tokens off the browser. Encrypt stored sensitive credentials with AES-256-GCM.
