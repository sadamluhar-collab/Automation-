# Recovery
Faults are classified into network, auth, database, queue, worker, provider, storage, media, pipeline, quota, rate-limit, YouTube, scheduler and related classes. Safe failures are retried/reconciled; destructive operations require user action.
