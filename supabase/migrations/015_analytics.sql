create table if not exists analytics(id uuid primary key default gen_random_uuid(),channel_id uuid not null references channels(id) on delete cascade,project_id uuid references projects(id) on delete cascade,youtube_video_id text,views bigint default 0,likes bigint default 0,comments bigint default 0,watch_time_seconds bigint default 0,observed_at timestamptz not null default now());
